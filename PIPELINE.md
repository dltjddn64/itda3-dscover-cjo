# 파이프라인 진행 문서

이 문서는 OCR 파이프라인이 어떻게 구성되어 있고, 어떤 순서로 개선되고 있는지 계속 업데이트됩니다.

## 전체 구조

```
원본 이미지 (data/raw/)
   ↓
[1] preprocess.py   : 이미지 로드, 리사이즈
   ↓
[2] ocr_engine.py   : EasyOCR로 텍스트 검출 + 인식 (이미지 → 텍스트 조각들의 위치+내용)
   ↓
[3] postprocess.py  : 텍스트 조각들 중 "진짜 소비기한"을 판별 + 날짜 형식 정리
   ↓
submission.csv (image_id, year, month, day, final_date)
```

`predict.ipynb`가 이 세 모듈을 순서대로 불러서 실행하는 최종 제출 파일입니다.

## 모듈별 현재 상태

| 모듈 | 역할 | 상태 |
|---|---|---|
| `src/preprocess.py` | 이미지 로드(한글 경로 대응), 리사이즈 | ✅ 완료 |
| `src/ocr_engine.py` | EasyOCR 래퍼, 이미지 → 텍스트박스 목록 | ✅ 완료 (베이스라인 그대로) |
| `src/postprocess.py` | 텍스트박스 목록 → 소비기한 판별 | 🔧 개선 중 (현재: 앵커 키워드 우선 + 첫 날짜 매칭 fallback) |
| `predict.ipynb` | 전체 파이프라인 연결 + CSV 저장 | ✅ 완료, 스펙 검증됨 |

## 진행 상황 로그

### 2026-09-08 — 베이스라인 이식 및 검증
- 대회 제공 베이스라인(`predict.py`)의 EasyOCR 로직을 `src/ocr_engine.py`, `src/postprocess.py`로 분리 이식
- `predict.ipynb`에 연결해서 공식 스펙(환경변수 입출력, `final_date` 컬럼 포함) 기준으로 동작하도록 완성
- 채점 서버와 동일한 Python 3.10 conda 환경(`itda`)을 새로 만들어 검증
- 샘플 이미지 5장(`data/sample_test/`)으로 `jupyter nbconvert --execute` 실행 → 에러 없이 통과, CSV 스키마 정확히 일치
- **결과: 5장 중 2장만 날짜 인식 성공** (000002, 000003) → 정확도 개선 필요 확인

### 2026-09-08 — 앵커 키워드 기반 판별 로직 추가
- `src/postprocess.py`에 `find_anchor_based_date()` 추가: "유통기한/소비기한/까지" 등 키워드가 포함된 텍스트를 우선 탐색하고, 같은 박스에 날짜가 없으면 위치상 가장 가까운 날짜 박스를 채택
- 앵커로 못 찾으면 기존 방식(첫 날짜)으로 fallback
- 샘플 5장으로 실제 OCR 인식 결과를 직접 찍어보며 디버깅 → **000001.jpg에서 날짜가 `2027,06.26`처럼 쉼표(,)로 오인식**되어 정규식이 못 잡던 버그 발견
  - OCR이 마침표(.)를 쉼표(,)로 잘못 읽는 경우가 있어, `DATE_PATTERNS`의 구분자에 `,`를 추가해서 해결
- **재검증 결과: 5장 중 3장 정확히 인식 성공** (000001, 000002, 000003) — 2장 → 3장으로 개선
  - 000004, 000005는 OCR 인식 품질 자체가 낮아(흐릿함/저해상도) 날짜 텍스트가 애초에 제대로 안 읽힘 → 정규식/후처리 로직이 아니라 **OCR 인식 단계**의 한계로, 추후 전처리(대비 향상, 업스케일) 또는 다른 OCR 엔진 검토 필요

### 2026-09-08 — 000004/000005 실패 원인 분석 및 줄 병합(line merge) 추가
- 처음엔 "사진 해상도가 낮아서"라고 추정했으나, 원본(3024x4032, 매우 선명)을 직접 봐도 문제 없었음 → **가설 기각**
- MAX_SIDE(1280/2000/3000/원본)를 바꿔가며 실험했지만 정확도 변화 없음 → 리사이즈 크기는 원인이 아니었음
- 실제 원인: OCR 디텍터가 `2022.02.14 16:01`처럼 한 줄짜리 날짜를 `2027`(오독) / `02.14` / `16.01` **3개의 별개 박스로 쪼개서 인식**하고 있었음. 좌표 확인 결과 세 조각 모두 같은 줄(y좌표 겹침)에 나란히 위치 → 기존 로직은 "한 박스 안에 완전한 날짜"만 인정해서 놓치고 있었음
- `postprocess.py`에 `merge_same_line_boxes()` 추가: y좌표가 겹치는 박스들을 같은 줄로 묶고, x좌표 순으로 이어붙여 하나의 텍스트로 재구성 → 이 병합된 텍스트도 날짜 탐색 대상에 포함
- **재검증 결과: 5장 중 4장 인식 성공** (000001~000004), 3장 → 4장으로 개선
- ⚠️ 단, 000004는 연도가 `2027`로 잘못 나옴 (원본은 `2022`) — OCR이 숫자 `2`를 `7`로 오독한 것으로, 줄 병합으로는 해결 안 되는 별개의 **문자 인식 정확도 문제**
- 000005는 여전히 실패: 텍스트가 애초에 한 박스에서 `226.07,&`처럼 뭉개져서 인식됨(신뢰도 0.12로 매우 낮음). 병뚜껑 곡면 반사광 때문으로 추정 — 전처리(대비 보정 등)로 개선 가능한지는 추가 확인 필요

### 2026-09-08 — 속도 최적화 (채점 타임아웃 대응)
- 공식 채점 명령어에 `--ExecutePreprocessor.timeout=2400`(40분)이 명시되어 있는데, `predict.ipynb`의 추론 루프가 셀 하나로 되어 있어 이 시간 안에 3,352장을 못 끝내면 **셀 타임아웃으로 강제 종료 → submission.csv 미생성 → 정량 60점 전부 0점** 위험 발견
- 최적화 전 속도: 장당 약 1.8초 → 3,352장 기준 약 100분 (타임아웃의 2.5배, 매우 위험)
- **멀티프로세싱(4~10 워커) 시도했으나 효과 미미** (100분 → 83분): torch가 기본적으로 4스레드를 내부적으로 이미 활용 중이라, 프로세스를 더 늘려도 실제 코어 활용은 크게 늘지 않음. 오히려 노트북(nbconvert) 실행 환경에서 멀티프로세싱은 안정성 리스크도 있어 보류
- **리사이즈 크기(MAX_SIDE) 축소가 훨씬 효과적**: 1280 → 640으로 낮추자 계산량이 줄어 장당 0.58초로 단축
  - 정확도 영향 거의 없음: 20장 샘플에서 1280일 때 9/20, 640일 때 8/20으로 오차 범위 수준 (480까지 낮추면 5/20으로 눈에 띄게 나빠짐)
  - 기존에 확인했던 5개 샘플(000001~000005)도 640에서 동일한 결과 유지 (4/5 성공)
- `src/preprocess.py`의 `MAX_SIDE`를 `1280 → 640`으로 변경
- **최종 검증(200장, 전체 파이프라인 기준): 장당 0.58초 → 3,352장 예상 약 32.5분(1,952초)**, 타임아웃(2400초) 대비 약 450초(19%) 여유

⚠️ **주의**: 위 시간은 로컬 맥북(10코어) 기준이며, 채점 서버는 클라우드 4코어 vCPU라 코어당 성능이 더 낮을 수 있음. 여유가 넉넉한 편은 아니라서 안전 마진을 더 확보할지 판단 필요.

## 다음 할 일 (TODO)

- [x] `postprocess.py`에 앵커 키워드(`유통기한`, `소비기한`, `까지` 등) 기반 판별 로직 추가
- [x] 한 줄 날짜가 여러 박스로 쪼개지는 문제 → 같은 줄 병합 로직 추가
- [ ] 숫자 오독 문제(`2022`→`2027`) 대응 방안 검토 — 난이도 높음, 후순위
- [x] ~~반사광/저대비 이미지 전처리(CLAHE) 실험~~ → 테스트 결과 악화, 채택 안 함
- [x] 제조일자/소분일/부터(시작일) 등 다른 종류 날짜 구분 로직 추가 (부정 키워드 필터 + 같은 줄 우선 탐색)
- [x] 검증셋(라벨링 150장) 대상 정확도 측정 완료 — recall 59.7%, precision 93.3%
- [x] CPU 추론 속도 측정 및 최적화 (타임아웃 2400초 대응)
- [x] 채점 기준 정정 확인: 500장/40분 (3,352장 아님) — 속도 여유 충분히 확보됨
- [ ] 채점 서버(4코어 vCPU)가 로컬보다 느릴 가능성 대비 — 안전 마진 재확인 (500장 기준으론 여유 커서 우선순위 낮음)
- [x] 날짜 정규식이 DD.MM.YYYY(수입상품 표기) 못 읽던 문제 수정 → recall 59.7%→69.8%
- [x] 608장 대규모 검증에서 부정키워드 유사매칭 버그 발견/수정 → recall 67.5%→74.0%
- [x] 평가 스크립트 파일 확장자(.jpg 고정) 버그 수정 (실제 predict.ipynb는 처음부터 다양한 확장자 지원해서 무관)
- [ ] 남은 recall 26% 개선 방안 검토 (남은 실패는 순수 탐지/인식 품질 문제로 보임 — 로직 수정 수확 체감, 이미지 처리 또는 엔진 자체 한계일 가능성)
- [ ] 채점 시 오답 페널티 여부 확인 (안내서에 명시 없음, 카카오 Q&A 문의 필요) — 확인되면 recall/precision 트레이드오프 전략 조정

### 2026-09-08 — 랜덤 샘플 전수 테스트 결과: 탐지율 9.7% (심각)
- `data/raw`에서 순서대로 뽑은 200장은 80장(40%)에서 날짜를 찾았지만, **완전 랜덤 300장으로 테스트하니 29장(9.7%)만 찾음**
  - 처음 200장(파일명 순서상 앞쪽)이 우연히 쉬운 샘플들이었던 것으로 보임 → 앞으로는 반드시 랜덤 샘플링으로 검증할 것
- NONE으로 나온 케이스 직접 이미지 확인 결과, 원인이 최소 두 가지로 나뉨:
  1. **탐지(Detection) 자체가 실패**: `002717.jpg`는 포장지 상단에 `2022.03.01`이 육안으로 또렷하게 보이는데도 EasyOCR이 그 영역을 아예 박스로 잡지도 못함. 리사이즈 문제인지 확인하려고 640/1280/원본 해상도 다 테스트했지만 **어느 해상도에서도 탐지 안 됨** → 리사이즈 문제가 아니라 EasyOCR 디텍터 자체의 한계 (빨간 줄무늬 배경 위에 작게 눌린 인쇄체라 텍스트로 잘 안 잡히는 것으로 추정)
  2. **사진 자체가 어려움**: `000106.jpg`는 요거트 뚜껑을 회전된 각도로 찍었고, 배경 소품들도 초점이 안 맞음. 소비기한으로 보이는 점자프린트가 우측 상단에 작게 회전된 채로 찍혀있어 인식이 사실상 불가능한 수준
- **결론**: 지금 baseline(EasyOCR 그대로 + 후처리 규칙)만으로는 recall이 매우 낮다. 안내서에서 "실무형 기술 문제"라고 강조한 이유가 이해되는 지점 — 전체 3,352장에 대해 근본적인 개선이 필요 (탐지 파라미터 튜닝, 다른 엔진과의 비교, 또는 애초에 recall보다 "확실한 것만 맞히고 나머지는 NONE" 전략을 취할지 방향 결정 필요)

### 2026-09-08 — PaddleOCR과 비교 실험 (결론: EasyOCR 유지)
- 같은 랜덤 40장으로 공정 비교:

| 설정 | 탐지율 | 장당 속도 | 3,352장 예상 |
|---|---|---|---|
| EasyOCR (640) | 15.0% (6/40) | 0.69s | 38.6분 |
| PaddleOCR mobile (640) | 30.0% (12/40) | 1.98s | 110.8분 |
| PaddleOCR mobile (480) | 30.0% | 1.40s | 78.0분 |
| PaddleOCR mobile (320) | 20.0% | 0.76s | 42.5분 |

- PaddleOCR이 정확도는 2배 가까이 높지만, 속도를 예산(40분)에 맞추려면 리사이즈를 320까지 줄여야 하고 그마저도 로컬 기준 42.5분으로 빠듯함 (채점 서버가 더 느릴 수 있다는 리스크 고려하면 위험)
- **결정: EasyOCR(640) 유지.** 타임아웃 시 정량 60점 전체 손실이라는 리스크가 정확도 15%p 차이보다 크다고 판단. 정확도는 다른 방법(탐지 파라미터 튜닝 등)으로 개선 예정
- PaddleOCR 관련 패키지(`paddlepaddle`, `paddleocr`)는 `itda` 환경에 설치는 되어 있음 (나중에 재검토 시 참고)

### 2026-09-08 — "찾음" ≠ "정답": 랜덤 4개 눈으로 대조 검증
- low_text=0.3으로 찾아낸 날짜 4개를 실제 사진과 직접 대조 → **2개는 틀림**
  1. `000218.jpg`: 실제 `2026.05.03`인데 `2026-05-08`로 추출 — **숫자 오독** (3→8, 모양이 비슷해서). 근본적으로 고치기 어려운 문제라 보류
  2. `000558.jpg`: 실제 라벨엔 "**소분일**"(재포장일, 소비기한 아님) `2025.05.16`이라고 적혀있는데 이걸 소비기한으로 착각해서 추출. **이건 고칠 수 있는 문제**
- confidence 점수로 오답을 거를 수 있는지 확인 → **안 됨** (틀린 것 conf=0.76이 맞는 것 conf=0.65보다 오히려 높음)

### 2026-09-08 — 부정 키워드 필터링 추가 ("소분일/제조일자" 오탐 제거)
- `src/postprocess.py`에 `NEGATIVE_KEYWORDS`(제조일자/생산일자/소분일/포장일/충전일) 추가, 날짜 후보 근처에 이 키워드가 있으면 fallback 채택에서 제외
- 흥미로운 점: OCR이 "소분일"을 "**소문일**"로 오독(분→문)해서 정확히 문자열이 일치하지 않음 → 편집거리 1 이하까지 허용하는 `_fuzzy_contains()` 퍼지 매칭을 구현해서 해결
- `src/ocr_engine.py`에 `low_text=0.3`을 정식 반영 (지금까지는 테스트 스크립트에서만 임시로 썼음)
- 재검증(같은 40장): `000558.jpg`는 정상적으로 NONE 처리됨. 단, 640 저해상도에서는 이미지별로 텍스트 쪼개짐 양상이 달라져서 `000001/000004` 같이 전에 맞았던 케이스가 이번엔 텍스트가 다르게 잘려서 실패하는 경우도 발견 (제 필터 로직 버그가 아니라 low_text 파라미터 자체의 이미지별 편차) — 이런 편차는 어느 정도 감수해야 함
- **최종(40장 기준): 22.5%(9/40) 탐지, 장당 0.68s, 3,352장 예상 38.0분** (이전 25%에서 소폭 감소했는데, 이건 성능 저하가 아니라 오탐 1건(000558)을 정확히 걸러낸 결과)

### 2026-09-08 — 이미지 전처리(CLAHE 대비 향상) 실험 → 채택 안 함
- 지금까지 전처리는 리사이즈뿐이었음. "화질 개선(대비 향상 등)을 하면 어려운 케이스들이 풀리지 않을까?" 가설을 테스트
- CLAHE(적응형 히스토그램 평활화)를 LAB 색공간의 명도(L) 채널에 적용 후 OCR 실행
- 기존 실패 케이스 3개(`002717`, `000106`, `000005`)에 적용 → **셋 다 여전히 실패, 개선 없음**
- 40장 랜덤 샘플 전체에 적용 → **오히려 악화** (22.5% → 15.0%)
  - 기존에 맞던 4개 케이스가 CLAHE 적용 후 실패로 바뀜 (`000363`, `000028`, `000422`, `000587`)
  - 심지어 이미 고쳤던 `000558`(소분일 오탐) 버그가 다시 재현됨 — CLAHE로 이미지가 바뀌면서 "소분일" 라벨 텍스트 자체가 인식이 안 돼 부정 키워드 필터가 무력화됨
- **결론: 이미지 전체에 일괄 대비 향상을 적용하는 방식은 채택하지 않음.** 오히려 노이즈를 키우거나 기존에 잘 되던 케이스를 방해하는 부작용이 더 큼. "전처리를 안 한 것"이 아니라 "테스트해보고 효과 없어서 안 쓰기로 결정한 것"

### 2026-09-08 — 2단계 크롭 재인식(검출 640 + 인식만 원본 해상도) 실험 → 채택 안 함
- 아이디어: 640에서 찾은 날짜 후보 박스 좌표를 원본 이미지로 역산해서, 그 부분만 원본 해상도로 크롭 후 숫자 전용(allowlist)으로 재인식하면 다운스케일로 인한 숫자 오독을 줄일 수 있지 않을까 하는 가설
- **1차 시도 (조각별 개별 재인식)**: 숫자 2개 이상 포함된 모든 박스를 각각 크롭+재인식 → 오히려 심하게 악화 (22.5%→12.5%). 인접한 조각끼리 크롭 영역이 겹쳐서 서로 오염되거나, allowlist 제약 때문에 노이즈에서도 억지로 숫자를 뽑아내는 부작용 발생
- **2차 시도 (선택된 후보 1개만 재인식)**: 기존 로직으로 이미 결정된 "정답 후보" 박스 하나만 원본 해상도로 재크롭+재인식. 찾은 개수는 동일(9/40)했지만, 값이 바뀐 유일한 1건(`000363.jpg`)을 실제 사진과 대조해보니 **원래 640 인식이 맞았고(2026.04.21) 재인식이 틀리게 바꿔버림(2025.04.21)** — 정답→오답 역행
- **결론: 크롭 재인식 방식 채택 안 함.** 두 가지 구현 모두 시도했지만 실제로는 도움이 안 되거나 오히려 해로웠음. `reader.recognize()`로 작은 크롭을 단독으로 인식시키는 것이 전체 이미지 문맥 안에서 인식하는 것과 다르게(더 나쁘게) 동작하는 것으로 추정 — 원인 미상, 추가 조사 필요하면 나중에 재검토
- 검출 자체가 실패하는 케이스(`002717` 등)는 원본 해상도로 재인식해도 애초에 박스가 없어서 대상이 안 됨 → 이 방법으로는 처음부터 해결 불가능한 문제였음

### 2026-09-08 — mag_ratio 및 캐스케이드(2단계 재시도) 실험 → 둘 다 채택 안 함
- **mag_ratio(검출 전 내부 업스케일)**: 002717/000106/000005 세 하드케이스에 1.0/1.5/2.0 테스트 → **셋 다 어떤 값에서도 개선 없음**, 속도만 최대 4배(0.5s→2.1s) 증가. 이 데이터셋의 "탐지 자체 실패" 유형에는 안 통하는 것으로 보임
- **캐스케이드(1차 640 빠르게 → 실패한 것만 2차 1280 재시도)**: "실패한 것만 비싼 재시도"라는 아이디어 자체는 합리적이지만, 지금 1차 실패율이 77%(31/40)로 너무 높아서 사실상 대부분의 이미지를 두 번 처리하는 꼴이 됨
  - 결과: 40장 중 1개 추가 발견(22.5%→25.0%), 대신 시간이 37.5분→94분으로 폭증(예산 40분 초과)
  - **결론: recall이 낮은 지금 상황에서는 캐스케이드가 오히려 손해.** 이 전략은 "대부분 성공, 소수만 실패"할 때 효과적인데 지금은 반대 상황이라 성립 안 함
- 결과적으로 이번 세션에서 시도한 "새 아이디어" 4개(크롭재인식 2종, mag_ratio, 캐스케이드) 전부 채택 안 함 — 전부 시도했다는 것 자체가 성과이자 대회 보고서에 쓸 수 있는 근거(왜 이 방법들을 안 썼는지)

### 2026-09-08 — 채점 기준 정정: 500장/40분 (3,352장 아님) → PaddleOCR 재도입
- 대회 측 문의 결과, 채점은 **전체 3,352장이 아니라 비공개 500장 기준으로 40분** 안에 처리하면 됨을 확인
- 그동안 "3,352장/40분(장당 0.7초)" 기준으로 최적화해왔는데, 실제로는 "500장/40분(장당 4.8초)" — 장당 예산이 **약 7배** 여유로움
- 이 여유 예산으로 그동안 속도 때문에 기각했던 PaddleOCR을 재검토:

| 설정 | 탐지율(40장) | 장당 속도 | 500장 예상 |
|---|---|---|---|
| EasyOCR (640, low_text=0.3, 전체 후처리) | 22.5% | 0.68s | 5.7분 |
| PaddleOCR (server det + korean mobile rec, 640, 전체 후처리) | 35.0% | 3.33s | **27.8분** |
| PaddleOCR (960) | 35.0% (동일) | 4.49s | 37.4분 (해상도 올려도 정확도 안 늘고 시간만 증가) |

- **결정: PaddleOCR(640)으로 엔진 교체.** `src/ocr_engine.py`에 `PaddleEngine` 클래스 추가 (기존 `OCREngine`과 동일한 인터페이스라 `postprocess.py`는 무수정)
- 탐지율 22.5%→35.0%로 대폭 개선, 속도도 27.8분으로 예산(40분) 안에 여유 있게 들어옴

### 2026-09-08 — 새 오탐 유형 발견: "부터"(시작일) vs "까지"(종료일)
- PaddleOCR 결과 중 `000422.jpg`를 실제 사진과 대조 → 라벨에 `2025.09.10 부터 ~ 2026.06.09 까지` 두 날짜가 있는데 **시작일(2025.09.10)을 소비기한으로 잘못 채택**
- `NEGATIVE_KEYWORDS`에 "부터" 추가를 시도했으나 확인해보니 **OCR이 "부터"/"까지" 글자 자체를 인식하지 못해서** 키워드 필터가 애초에 작동할 대상이 없었음 (탐지 단계 한계, 로직으로 못 고침)
- 대신 더 근본적인 규칙 적용: **앵커 키워드로 못 찾을 때, "첫 번째로 찾은 날짜"가 아니라 "가장 늦은(미래) 날짜"를 채택**하도록 fallback 로직 변경. 소비기한은 항상 제조일자·시작일보다 미래이므로, 키워드 인식 여부와 무관하게 통하는 더 안전한 기본값
- 재검증: `000422.jpg`가 `2026-06-09`(정답)로 수정됨, 40장 기준 다른 결과는 전혀 안 깨짐 (14/40, 35.0% 유지)

### 2026-09-09 — 탐지율 추가 개선 시도 3종 → 전부 기각, 현재 설정이 최선으로 확인
- **앙상블(EasyOCR ∪ PaddleOCR, 둘 중 하나라도 찾으면 채택)**: 35.0%→37.5%로 +1장뿐. 알고보니 두 엔진이 거의 같은 사진에서 성공해서 겹침이 큼(합집합 15장 중 5장만 "둘 다 성공"). EasyOCR만 찾은 유일한 1장(`000218`)은 이미 알려진 오독 케이스(정답 03인데 08로 읽음)라 사실상 순이득 없음. 속도만 27.8분→33.3분으로 늘어서 기각
- **Paddle 후처리 결과가 raw보다 1장 적은 이유 확인**: 버그가 아니었음 — 사라진 1장(`000558`)은 예전에 고쳤던 "소분일" 오탐 케이스를 부정 키워드 필터가 정확히 걸러낸 것. 즉 35.0%가 37.5%보다 실제로 더 신뢰할 수 있는 숫자
- **Paddle 탐지 민감도 튜닝**(`text_det_thresh`, `text_det_box_thresh`, `text_det_unclip_ratio`): EasyOCR의 `low_text` 튜닝처럼 효과를 기대했으나 **오히려 악화**(35.0%→32.5%, 두 가지 조합 모두). PP-OCRv5 서버 디텍터는 기본값이 이미 잘 튜닝되어 있어서, 민감도를 더 낮추면 EasyOCR과 달리 노이즈만 늘어나는 것으로 보임
- **결론: 현재 설정(PaddleOCR 기본 설정 + 640 + 전체 후처리)이 지금까지 시도한 것 중 최선.** 35.0%(40장 샘플), 27.8분(500장 예상)

### 2026-09-09 — 라벨링된 검증셋 150장 구축 (자체 라벨링 가산점 겸용)
- 팀원 제안대로, 40장 랜덤 샘플로는 노이즈(±7%p)가 너무 커서 개선 여부를 판단할 수 없다는 문제를 해결하기 위해 **랜덤 150장을 직접 눈으로 읽어서 정답 라벨링**
- `data/labels/validation_labels.csv`에 저장 (image_id, year, month, day, final_date, note)
- 9장씩 그리드 이미지로 묶어서 읽는 방식으로 효율화 (개별로 열지 않고 한 번에 여러 장 확인)
- 139장은 육안으로 확실히 읽음, 11장은 형식이 모호하거나(로트코드 vs 날짜) 화질이 나빠서 NONE 처리
- **부수 효과 1**: 라벨링 도중 실제 소비기한이 **2020~2022년도인 경우가 매우 흔함**을 확인 → 기존 `YEAR_MIN=2023`이 이런 진짜 정답들을 원천 차단하고 있었음. `YEAR_MIN, YEAR_MAX = 2015, 2032`로 확장 (즉시 반영, 통계적 검증 필요 없는 명백한 버그였음)
- **부수 효과 2**: 대회 요구사항인 "데이터 직접 수집/라벨링" 가산점 항목 동시 충족

### 2026-09-09 — 검증셋 기준 실제 정확도 측정: 59.7% (recall), 93.3% (precision)
- 지금까지 "40장 중 35% 찾음" 같은 숫자는 사실 "찾음=정답"이 아니었음. 처음으로 진짜 정답과 비교한 결과:

| 지표 | 값 |
|---|---|
| 정답 있는 139장 중 정확히 맞춘 비율 (recall) | 59.7% (83/139) |
| 찾아낸 것들 중 정확도 (precision) | 93.3% (83/89) |
| 애매해서 NONE으로 라벨링한 11장 중 오탐 | 0건 |

- 즉 지금 파이프라인은 **"확신 없으면 억지로 답 안 냄" 성향이 강하고(정밀도 높음), 못 찾는 비율(recall)이 아직 40% 정도**라는 게 처음으로 숫자로 확인됨
- 오답 7건 분석 → 패턴 2가지 발견:
  1. **연도 숫자 오독** (2022→2027, 2021→2022 등): 근본적으로 고치기 어려움, 보류
  2. **"부터"(시작일)~"까지"(종료일) 표 레이아웃에서 앵커 매칭이 엉뚱한 날짜를 고름**: 원인은 두 가지 버그였음
     - `merge_same_line_boxes`가 "25.09.02 부터 까지"처럼 서로 다른 라벨을 한 줄로 잘못 합쳐서, 그 안에 있는 날짜를 "같은 박스 안에 앵커+날짜"로 바로 채택해버림 → 이 경로에 부정 키워드 체크 추가해서 차단
     - 라벨:값이 좌우로 넓게 배치된 표 형태에서, 다른 줄이지만 세로로만 가까운 오답이 순수 유클리드 거리로는 정답보다 더 "가깝게" 계산됨 → 세로 위치가 비슷한(같은 줄) 후보를 먼저 찾고, 없을 때만 전체 최근접으로 대체하도록 `find_anchor_based_date` 수정
     - 수정 후 재검증: 83/139(59.7%), precision 93.3%로 소폭 개선, 회귀 없음. 단 행 간격이 극도로 좁은 사진 1~2건은 여전히 어려움 (더 정교한 레이아웃 분석 필요, 수확체감 판단하여 보류)

### 2026-09-09 — 놓친 50장 유형 분석 → 날짜 정규식이 DD.MM.YYYY(수입상품 표기)를 아예 못 읽고 있었음 발견 (이번 세션 최대 성과)
- 검증셋 기준 못 찾은 50장을 그리드로 묶어서 직접 눈으로 확인
- 상당수가 **탐지/화질 문제가 아니라, OCR은 제대로 읽었는데 정규식이 그 형식을 모르는 경우**였음:
  - `PROD. 22.06.2020` / `13/09/2020` / `PRD DATE : 25 07 2020` 처럼 **일(day)이 먼저 오는 수입상품 표기(DD.MM.YYYY)** — 기존 정규식은 `(\d{4})[sep](\d{1,2})[sep](\d{1,2})` 형태로 **연도가 항상 맨 앞에 온다고만 가정**하고 있어서 이런 형식은 애초에 매칭 자체가 안 됐음
  - `2026. 9.24`처럼 **구분자가 마침표+공백 두 글자**인 경우 — 정규식 구분자 클래스가 한 글자만 허용해서 매칭 실패
- `src/postprocess.py` 수정:
  - `DATE_PATTERNS`를 `(정규식, 그룹순서)` 튜플 리스트로 재구성, 일(day)이 먼저 오는 `DD.MM.YYYY` 패턴 추가 (연도가 4자리라 순서상 모호함 없이 구분 가능)
  - 구분자 클래스를 `[.\-/,\s]` → `[.\-/,\s]+`(한 글자 이상)로 변경해 다중 구분자 문자 허용
- **검증셋 150장 재측정: recall 59.7%→69.8%(+10.1%p), precision 93.3% 그대로 유지, 오탐 여전히 0건.** 이미지 처리 트릭 없이 순수 정규식 수정만으로 이번 세션 최대 폭의 개선
- 남은 8개 중 3개(`000977`,`002436`,`000867`)를 직접 디버깅해보니 정규식이 아니라 **OCR 인식 자체가 심하게 깨짐**(회전/저품질 인쇄) — 이건 처음에 팀원이 짚었던 "방향(rotation)" 축 문제와 연결되는 별개 사안, 후속 과제로 분리

### 2026-09-10 — 팀원이 추가한 500장 라벨 데이터 검토 → 파서 추가 개선
- `ITDA_500_image_date_candidates.csv` (image_id, filename, date_candidates_raw) 500행 확인 — 기존 검증셋 150장과 겹치지 않는 새 데이터, 이미지 파일 500개 전부 존재 확인
- 형식이 자유로워서(엑셀에서 작성, BOM 인코딩) 사람마다 다른 표기法(`2026.10.29`, `27.04.2026`, `AUG292020`, `30 12 23` 등) 섞여 있음
- 멀티 후보(85건) 대부분이 `2025.xx.xx | 2026.xx.xx` 패턴 → 기존에 다루던 "부터~까지" 쌍으로 확인, 저희 "최댓값 채택" 규칙을 그대로 적용
- 저희 `parse_first_date()`로 그대로 돌려서 자동 정리 시도 → 1차 425/500(85%) 해결
- 실패 사례 분석해서 파서에 3가지 추가:
  1. 일-월-2자리연도 (`13/03/22`, `04-11-23`)
  2. 영문 월 이름 표기 (`MAY 12 2026`, `07FEB2022`, `FEB/26/21`) — `_MONTH_NUM` 매핑 추가
  3. 일/월이 뒤바뀐 미국식(`4.13.2026`)도 1차 실패 시 순서 바꿔 재시도
  - 이 과정에서 **"Nov 2021"(연도만 있고 일자 없음)을 "20"+"21"로 잘못 쪼개 억지로 답을 만들어내는 버그**를 발견해서 각 숫자 그룹 뒤에 `(?!\d)` 경계를 추가해 수정 (자릿수 넘치는 매칭 방지)
- 최종 458/500(91.6%) 자동 해결. 남은 42개는 대부분 **연도 또는 일자 자체가 원본에 없는** 경우(`10.14`, `2026.01`, `OCT.2021` 등)라 규칙으로 복원 불가 — 수동 확인 필요 항목으로 별도 표시
- **검증셋(150장) 재측정 결과: recall 69.8%→71.9%, precision 93.3%→92.6%(오탐 1건 발생, 다만 이건 원래 저희가 라벨링 때 "불확실"로 표시했던 이미지라 진짜 오답인지 애매함)**
- 결과물: `data/labels/500_candidates_resolved.csv` (자동 해결 458 + 수동확인 필요 42)

### 2026-09-10 — 608장(150+458) 대규모 검증 → 치명적 버그 발견 및 수정 (recall 67.5%→74.0%)
- 놓친 케이스들을 다시 분석하다가 **`.jpg`가 아닌 파일(`.jpeg`, `.png`) 16개를 평가 스크립트가 파일 못 찾아서 전부 "못찾음"으로 잘못 집계**하고 있었음을 발견 → 수정 후 재측정: recall 66.5%→67.5%
- 놓친 케이스 샘플을 그리드로 다시 확인 → 대부분이 회전/반사광이 아니라 "육안으로는 멀쩡한데 그냥 안 잡히는" 케이스였음. 그 중 하나(`002818`)를 직접 디버깅했더니 **OCR이 날짜를 신뢰도 100%로 정확히 읽었는데도 최종 결과가 NONE으로 나오는 심각한 버그**를 발견:
  - 원인: 부정 키워드 유사매칭(편집거리 1)이 "충전일"과 "충전제"(식품첨가물, 날짜와 무관)를 혼동해서 근처의 정답 날짜를 오탐으로 걸러냄. 짧은 키워드에 편집거리 허용을 너무 느슨하게 적용한 게 원인
  - 수정: 유사매칭은 실제 OCR 오독이 확인된 "소분일"에만 적용, 나머지는 정확히 일치할 때만 부정 키워드로 인정
  - 추가로 OCR이 마침표를 콜론으로 잘못 읽는 경우(`2022.11:02`)도 구분자에 추가
  - **재검증: recall 67.5%→74.0%(+6.5%p), 587장 기준 426/576 정답**
- 위 수정으로 오탐 3건 발생 → 확인해보니 2건은 제 라벨링 자체가 "불확실"이었던 애매한 케이스, 1건(`002312`, "제조2020.08.17"을 제조일자로 못 거름)은 진짜 버그
- `NEGATIVE_KEYWORDS`에 "제조"를 단독으로 추가해서 고쳐봤으나 **너무 광범위해서 역효과** — "제조원"/"제조사"/"제조번호" 등 무관한 텍스트까지 다 걸려서 정답 21개가 억울하게 사라짐 (recall 74.0%→70.3%로 오히려 악화). **되돌림.** 교훈: 짧은 키워드를 부정 목록에 넣을 때는 반드시 전체 검증셋으로 순효과를 확인해야 함 — 버그 하나 고치려다 더 큰 손해를 볼 수 있음
- **최종 확정 상태: recall 74.0%, precision 88.9% (오탐 3건, 587장 기준)**

### 2026-09-11 — 놓친 케이스 추가 디버깅 + textline_orientation 테스트 → 로직 버그는 소진, 남은 건 진짜 인식 품질 문제
- 002818 버그 발견 이후 방식 그대로, "육안으로는 멀쩡한데 놓친" 케이스 몇 개 더 직접 디버깅
  - `001263`, `002414`: 재검증해보니 이미 어제 수정(콜론 구분자, 부정키워드 정확매칭)으로 **이미 고쳐져 있었음** — 별도 조치 불필요
  - `003015`: OCR이 "2022.07.0"으로 마지막 숫자를 아예 잘라먹음(day=0, 유효하지 않은 날짜) — 인식 단계 손실이라 로직으로 복구 불가
  - `000862`: 저희가 추가한 "월/일 순서 바꿔 재시도"(mdy fallback) 로직이 `2:18:2029`라는 무관한 숫자열을 그럴듯한 날짜로 잘못 조합한 사례 발견. 드문 사례라 즉시 되돌리진 않았지만, 이 fallback이 가끔 오탐을 만들 수 있다는 리스크로 기록
- **팀원이 처음 제안했던 회전(rotation) 대응책 — Paddle의 `use_textline_orientation` 옵션을 이제서야 테스트**: 회전된 것으로 보이는 케이스(`000229`,`002881`,`000106`,`002436`) 4개에 켜고/꺼서 비교
  - 결과: **오히려 악화.** 잘 찾던 `002881`(2025-09-14)이 옵션 켜니 NONE으로 바뀜, 나머지는 켜도 꺼도 그대로 NONE. 속도만 소폭 증가
  - 결론: 이 데이터셋에서는 방향 보정이 실질적 회전보다 오탐지를 더 유발하는 것으로 보임. **채택 안 함**
- **정리: 로직/파라미터 레벨에서 시도할 수 있는 것은 이제 거의 소진된 상태.** 남은 미탐지 케이스들은 순수 인식 품질 문제(자릿수 손실, 극단적 저대비, 디텍터가 원천적으로 못 잡는 인쇄 스타일)로 보이며, 추가 개선은 엔진 자체를 바꾸거나(파인튜닝 등, 큰 공수) 현재 수준을 받아들이는 것 중 선택의 문제로 보임

### 2026-09-11 — 채점 기준 추가 확인: 연/월/일 필드별 부분점수 존재 → 파이프라인 구조 변경
- 대회 측 확인: **연/월/일을 각각 채점하며 부분점수가 있음.** 모르는 필드는 통째로 NONE 처리하지 말고 "NONE-MM-DD"처럼 아는 필드만 채워서 제출해야 함
- 기존 구조는 "완전한 날짜 3개 다 찾아야 답하고, 하나라도 없으면 통째로 NONE" — 부분점수 제도 하에서는 손해 보는 구조였음
- `src/postprocess.py`에 부분 추출 경로 추가:
  - `parse_year_month()`: 연+월만 있고 일이 없는 경우 (`2026.01`, `OCT.2021`, `12/2020` 등, 월-연도 순서 둘 다 지원)
  - `parse_month_day()`: 연도 없이 월+일만 있는 경우 (`10.14` 등)
  - `find_anchor_based_partial()`: 완전한 날짜를 못 찾았을 때 앵커("유통기한"/"까지" 등) 근처에서 부분 정보 탐색. **월+일(연도없음) 패턴은 반드시 앵커 근처로만 제한** — 가격/전화번호 등 무관한 숫자쌍과 충돌 위험이 커서
  - 연+월은 4자리 연도가 필수라 오탐 위험이 낮으므로, 앵커 없이 라벨 전체를 뒤지는 별도 fallback도 추가 (월+일 fallback은 위험해서 앵커 근처로만 유지)
  - `extract_expiry_date()`: 완전 매칭 실패 시 부분 매칭으로 순차 폴백, 결과에 `"NONE"`을 필드별로 섞어서 반환 가능하도록 변경
- **검증(587장) 결과**: 완전일치 정답은 그대로 426장 유지, 그중 16장이 새로 부분점수(예: `2022-07-NONE`)를 받게 됨. **필드 단위 정답률 78.1%(1350/1728)** — 이게 이제부터 추적해야 할 핵심 지표
- 부분 답변으로 인해 "NONE 라벨인데 값을 냄" 케이스가 8건(필드 기준 19개) 나와서 전수 확인함 → **5건은 실제로 제가 라벨링할 때 놓쳤던 진짜 정답/부분정답이었고 (제 라벨 오류), 1건은 이미 알던 트레이드오프, 2건은 원래도 애매했던 케이스.** 새 로직이 만든 진짜 오탐은 사실상 없었음. 다만 이 과정에서 **검증셋 라벨 자체에 품질 이슈**(부분점수 개념 도입 전이라 애매하면 다 NONE으로 보수적으로 처리했던 것)가 있다는 걸 발견 — 여유 있으면 재검토 필요, 지금은 우선순위 낮음

### 2026-09-11 (추가) — 영문 월 이름 "연도-월-일" 순서 지원 추가
- 팀원이 라벨링 중 `OCT`, `AUG` 등 영문 월 이름 표기를 다수 발견 — 기존에 `일-월이름-연도`, `월이름-일-연도` 순서는 지원했지만 `연도-월이름-일`(`2021 OCT 15`) 순서는 빠져있었음
- `MONTH_NAME_PATTERNS`에 `yMd` 순서 패턴 추가, `parse_first_date()`에 해당 그룹 순서 처리 추가
- 완전히 붙여쓴 `AUG292021`(구분자 없이 월이름+일+연도가 붙은 경우)은 여전히 미지원 — 일/연도 경계가 모호해서 안전하게 구분 불가 (`Nov 2021`처럼 일자 없는 경우와 충돌 위험, 9-10 항목 참고). 실제 사례 나오면 재검토
- 회귀 체크 통과, 기존 케이스 전부 정상 유지

### 2026-09-12 — 조직위, final_date 표기 규칙 공식 확인 (코드 변경 불필요)
- 조직위가 다음 규칙을 명시적으로 안내: year/month/day는 인식된 필드만 채우고 나머지는 각각 "NONE", `final_date`는 세 값을 하이픈으로 연결하되 **셋 다 NONE일 때만 예외적으로 "NONE" 하나로** 작성. month/day는 반드시 2자리(`05`, `09`)
- 어제(9/11) 부분점수 대응 구조로 바꾼 로직이 이미 이 규칙과 정확히 일치 — `extract_expiry_date()`가 전부 NONE일 때 `"NONE"`을 반환하고, 부분 인식 시 `"NONE-08-25"` 형태로 정확히 조립하는 것 실제 테스트로 재확인함. 추가 수정 없음

### 2026-09-14 — 조직위 공지: `custom_data/` 폴더 규격 신설 (가산점 심사용) → 라벨 데이터 이동
- 조직위가 가산점 심사를 위해 저장소 루트에 `custom_data/` 폴더 신설을 요청 (내부 구조 자유, 루트 README.md에 1~2줄 안내 필수, 파일당 100MB 제한)
- 기존 `data/labels/`(라벨 CSV들)와 루트의 `ITDA_500_image_date_candidates.csv`를 `custom_data/`로 이동 (`git mv`로 이력 보존)
  - `custom_data/labels/`: `master_labels.csv`(587장 통합), `validation_labels.csv`(직접 라벨링 150장), `500_candidates_resolved.csv`, `LABELING_GUIDE.md`
  - `custom_data/ITDA_500_image_date_candidates.csv`: 팀원이 수집한 500장 원본 후보
- `src/`, `predict.ipynb`는 이 경로를 참조하지 않아 이동으로 인한 실행 영향 없음(사전 grep으로 확인)
- 루트 `README.md`에 안내 문구 추가, `custom_data/README.md` 신규 작성, `LABELING_GUIDE.md` 내부 경로 참조 갱신
- 총 용량 92KB — 100MB 제한과 무관, 이미지 원본은 포함하지 않음(라벨은 `image_id`로 대회 제공 원본 이미지를 참조하는 방식이라 재업로드 불필요)

## 현재 알려진 한계 (정직하게 기록)

> ⚠️ 이 섹션은 2026-09-09에 처음 작성된 뒤 계속 방치됐던 스냅샷이라 아래 최신 로그(엔진 교체, 정규식 수정,
> 부분점수 대응 등)가 전혀 반영되어 있지 않았음. 2026-09-11 기준으로 갱신함 — 최신 수치는 항상
> 이 문서 뒤쪽의 최근 날짜 로그를 확인할 것.

**최신 확정 수치 (587장 검증셋, 2026-09-11 기준)**: 필드 단위(연/월/일 각각) 정답률 **78.0%** (1364/1749), 완전일치 428/587, 처리속도 500장 기준 약 25분(예산 40분)

**아직 남은 진짜 한계**
- 숫자 오독(2↔7, 3↔8 등 모양이 비슷한 숫자)은 해결 못함 — "찾은 것"도 일부는 틀릴 수 있음
- confidence 점수는 정답/오답 판별에 못 씀 (오답이 정답보다 confidence 높은 경우 실제로 관측됨)
- 회전/줄무늬배경/반사광처럼 순수 이미지 품질 문제로 인한 탐지 실패는 로직 수정으로 한계 도달 (mag_ratio, textline_orientation, CLAHE 등 다 시도했으나 개선 없음)
- 검증셋 라벨(608장) 자체에도 소량의 사람 실수가 있었음이 확인됨 (재검토 중 7건 발견/수정) — 완벽한 정답셋은 아님
- [x] 자체 라벨링 전략 수립 (가산점 항목) — 완료: 검증셋 608장 라벨링 (150 직접 + 458 팀원 라벨 자동 정리)

### 2026-09-15 — 조직위, 채점 환경 세부 사양 공지 → 3가지 컴플라이언스 수정 (마감일)
- 공지 내용: Ubuntu 22.04 x86_64, Python 3.10, CPU 4코어/RAM 8GB, GPU 없음, 오프라인, **팀별 순수 venv**(conda 아님)에서 `requirements.txt` 설치. 버전 정확히 고정 요구. **가중치 다운로드 코드는 `download_weights.sh`에만, `predict.ipynb`는 로컬 파일을 읽기만 해야 함**(명시적 금지)
- 점검 및 수정:
  1. **RAM 8GB 여유 확인**: 20장 처리 기준 peak memory 약 3.1GB로 측정, 여유 충분 (`/usr/bin/time -l`로 실측)
  2. **requirements.txt 버전 고정**: 기존 `>=` 범위 지정을 전부 `==` 정확한 버전으로 교체 (torch==2.14.0 등 실제 설치된 버전 기준)
  3. **`predict.ipynb` 내 암묵적 온라인 다운로드 폴백 제거**: `PaddleEngine.__init__`에 있던 "로컬 가중치 없으면 온라인 다운로드에 의존"하는 fallback 분기를 제거하고, 로컬 가중치가 없으면 즉시 `RuntimeError`로 멈추도록 변경 (조직위 신규 규정 문자 그대로 준수 — 노트북 안에 다운로드 가능성 자체를 없앰)
- **최종 검증**: conda 없이 순수 `venv`(`python3.10 -m venv`)를 새로 만들어 `pip install -r requirements.txt`로만 설치 → 네트워크 차단(프록시 무효화) 상태에서 `predict.ipynb` Run All 실행 → 정상 완주, 결과 정확함을 확인. 조직위가 설명한 채점 환경과 가장 가깝게 재현한 검증
